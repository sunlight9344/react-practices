import React from 'react';
import styles from './assets/scss/CardTitle.scss';

function CardTitle(props) {
    return (
        <div className={styles.CardTitle}>
            CardTitle
        </div>
    );
}

export default CardTitle;