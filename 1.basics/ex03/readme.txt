ex03: 전통적인 DOM 기반의 Application: JS module system을 사용한 파일 분리 II

1. ES6 module system을 지원하는 브라우저에서는 ES6 모듈 시스템 기반으로 분리된 JS 모듈간의
   의존성을 보장해준다.  <- import, export 로 해놨으니까

2. 자바스크립트 외에 다양한 assets(css(link로 가져옴),image, font)의 로딩 동기화는 불가능(firePossible)하다.

3. 